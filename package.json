{
  "name": "ecommerce",
  "version": "1.0.0",
  "description": "ecommerceMERN",
  "main": "index.js",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "florcitahnatiuk",
  "license": "ISC"
}
